<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.itwill.running.repository.CourseDao">
  	
  	<!-- Course 객체 생성 -->
  	<insert id="insertCourse">
  		insert into course (title, user_id, nickname, course_name, duration_time, 
  							content, category, view_count, like_count, created_time, modified_time)
  		values (#{title}, #{userId}, #{nickname}, #{courseName}, systimestamp,
  		 					#{content}, #{category}, #{viewCount}, #{likeCount}, systimestamp, systimestamp)
  	</insert>
  	
  	<!-- Course 객체 모두 검색 -->
  	<select id="selectCourseByAll" resultType="Course">
  		select * from course order by id
  	</select>
  	
  	<!-- 카테고리(추천,리뷰)로 Course 객체 검색 -->
  	<select id ="selectCourseByCategory" resultType="Course">
  		select * from course
  		<where>
  			<choose>
  				<when test='category.equals(0)'>
  					category = 0
  				</when>
  				<when test='category.equals(1)'>
  					category = 1
  				</when>
  			</choose>
  		</where>
  		order by id
  	</select>
  	
  	<!-- 좋아요순으로 Course 객체 검색 -->
  	
  	
  
  </mapper>